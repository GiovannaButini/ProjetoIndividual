var lista_cliquesCores = [];
var lista_cliquesTracos = [];
var lista_cliquesPintura = [];
var lista_cliquesCaricatura = [];
var lista_cliquesCoresQuadrinhos = [];
var lista_cliques = [];
function clicarCores(){
    var cliques = 0;
    lista_cliques.push(cliques);
    for (contadorCores = 0; contadorCores <= lista_cliquesCores.length; contadorCores++){
        console.log("ok");
        console.log(contador);
    }
}

function clicarTracos(){
    var cliques = 0;
    lista_cliques.push(cliques);
    for (contadorTracos = 0; contadorTracos <= lista_cliquesTracos.length; contadorTracos++){
        console.log("ok");
        console.log(contador);
    }
}

function clicarPintura(){
    var cliques = 0;
    lista_cliques.push(cliques);
    for (contadorPintura = 0; contadorPintura <= lista_cliquesPintura.length; contadorPintura++){
        console.log("ok");
        console.log(contador);
    }
}

function clicarQuadrinhos(){
    var cliques = 0;
    lista_cliques.push(cliques);
    for (contador = 0; contador <= lista_cliquesQuadrinhos.length; contador++){
        console.log("ok");
        console.log(contador);
    }
}
